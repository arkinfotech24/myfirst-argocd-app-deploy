# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: {{ .Release.Name }}-config
# data:
#   APP_MESSAGE: "Hello from myfirstapp via ConfigMap!"apiVersion: v1
# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: {{ .Release.Name }}-config
# data:
#   index.html: |
#     <html>
#     <body>
#       <h1>Hello from myfirstapp via ConfigMap!</h1>
#     </body>
#     </html>

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  index.html: |
    <html>
    <head>
      <title>MyFirstApp</title>
      <style>
        body {
          background-color: #f0f8ff; /* light blue background */
          font-family: Arial, sans-serif;
          text-align: center;
          padding-top: 50px;
        }
        h1 {
          color: #2e8b57; /* sea green */
          font-size: 2.5em;
        }
        h2 {
          color: #4682b4; /* steel blue */
          margin-top: 20px;
        }
      </style>
    </head>
    <body>
      <h1>Hello from myfirstapp via ConfigMap!</h1>
      <h2>🚀 Deployed with Argo CD & Helm</h2>
    </body>
    </html>
